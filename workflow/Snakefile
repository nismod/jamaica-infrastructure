configfile: "config.yaml"

import pandas as pd
import geopandas as gpd
import os

HAZARD_TYPES = ("TC", "flooding")

# assign globals to these paths, they're used frequently
RAW = config["paths"]["incoming_data"]
DATA = config["paths"]["data"]
OUTPUT = config["paths"]["output"]

wildcard_constraints:
    hazard="[^_/]+",  # no slash or underscore
    gpkg="[^/]+",  # no slash in filename
    protection_type="[^/]+",  # no slash
    layer="(nodes|edges|areas)",
    threshold=r"\d+(p\d+)?",
    chunk=r"\d+",
    output_path=f"{OUTPUT}(/.+[^/])?",
    parameter_set=r"parameter_set_\d+",
    sector=r"(water|energy|transport)",

include: "utilities/_utilities.smk"
include: "1_damage/_damage.smk"
include: "2a_transport/_transport.smk"
include: "3_criticality/_criticality.smk"
include: "4a_losses/_losses.smk"
include: "4b_hotspots/_hotspots.smk"
include: "5_adaptation/_adaptation.smk"
include: "6_etl/_etl.smk"

from check_targets import EXPOSURE, DAMAGES, LOSSES, EAD_EAEL, ADAPTATIONS, TARGETS

rule all_exposure:
    input: EXPOSURE

rule all_damages:
    input: DAMAGES

rule all_losses:
    input: LOSSES

rule all_ead_eael:
    input: EAD_EAEL

rule all_adaptations:
    input: ADAPTATIONS

rule all_targets:
    input: TARGETS
